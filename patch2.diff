--- a/scrapers/inserate_ultra_optimized.py
+++ b/scrapers/inserate_ultra_optimized.py
@@ -/* innerhalb ultra_optimized_scrape, vor der Pagination-Schleife */
             total_duplicates = 0
 
             # --------------------------------------------------------------
-            # Pagination loop.
-            # ----------------------------------------------------------
-            # Each call to ultra_optimized_fetch_page acquires its own
-            # BrowserContext from the pool and releases it when done.
-            # No persistent context is held across pages — this avoids
-            # Kleinanzeigen session accumulation that triggers
-            # ERR_TOO_MANY_REDIRECTS after ~6 sequential navigations.
-            # ----------------------------------------------------------
+            # Pagination loop.
+            # ----------------------------------------------------------
+            # Ein einziger BrowserContext wird für die GESAMTE
+            # Pagination-Session gehalten und an jeden
+            # ultra_optimized_fetch_page()-Aufruf durchgereicht.
+            # Kleinanzeigen erwartet Session-/Cookie-Kontinuität über
+            # aufeinanderfolgende Seitenaufrufe hinweg — ein neuer,
+            # cookie-loser Context pro Seite war die Ursache der
+            # ERR_TOO_MANY_REDIRECTS-Abbrüche nach ~5-6 Seiten.
+            # Der Context wird erst NACH Abschluss der kompletten
+            # Pagination wieder freigegeben (siehe unten, Cookies werden
+            # dabei geleert).
+            # ----------------------------------------------------------
+
+            session_context = await self.browser_manager.get_context()
 
             while True:
@@ -/* Aufruf von ultra_optimized_fetch_page innerhalb der Schleife */
                     try:
 
                         (
                             page_results,
                             page_metrics,
                             page_extras,
                             next_page_url,
                         ) = (
                             await self.ultra_optimized_fetch_page(
                                 url=current_page_url,
                                 page_num=page_num,
+                                context=session_context,
                             )
                         )
@@ -/* nach Ende der while-Schleife, vor "Final deduplication" */
+            # --------------------------------------------------------------
+            # Session-Context freigeben, jetzt wo die komplette Pagination
+            # abgeschlossen ist. Cookies werden dabei geleert (siehe
+            # OptimizedPlaywrightManager.release_context), damit spätere,
+            # unabhängige API-Requests keinen Session-State erben.
+            # --------------------------------------------------------------
+
+            try:
+                await self.browser_manager.release_context(session_context)
+            except Exception:
+                pass
+
             # --------------------------------------------------------------
             # Final deduplication
             # --------------------------------------------------------------
 
             all_results = (
                 _deduplicate_results(
                     all_results
                 )
             )